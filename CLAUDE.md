# CLAUDE.md

The canonical project guidance lives in @AGENTS.md — it is shared with other agents
(Codex, etc.). Read and maintain it there so every tool stays in sync; don't duplicate
its content here.

## Claude Code specifics

- Your per-user auto-memory (feedback/project notes under `~/.claude/…/memory/`) is
  **private to Claude Code** and is NOT visible to Codex or other tools. Durable,
  shareable project facts belong in `AGENTS.md`, not in private memory.
